# cloudquiz-serverless-quiz-application
A serverless quiz application built using AWS Lambda and Python. The application serves a responsive quiz interface and calculates user scores without managing any servers.
